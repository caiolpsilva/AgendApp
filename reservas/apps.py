from django.apps import AppConfig

# Define a configuração do aplicativo 'reservas'
class ReservasConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    
    # Nome do aplicativo
    name = 'reservas'
